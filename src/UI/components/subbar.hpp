#pragma once
#include "UI/AbstractSubmenu.hpp"
using namespace UserInterface;
namespace VX
{
	class SubbarComponents
	{
        
	private:
		
	public:
        void Init(AbstractSubmenu* sub);
	};

	SubbarComponents* GetSubbar();
}